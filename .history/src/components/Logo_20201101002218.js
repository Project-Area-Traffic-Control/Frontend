import React from 'react';
import a from '../../public'
const Logo = (props) => {
  return (
    <img
      alt="Logo"
      src="../../public/logo512.png"
      {...props}
    />
  );
};

export default Logo;
